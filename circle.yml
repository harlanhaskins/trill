machine:
  xcode:
    version: 9.0
  environment:
    LC_CTYPE: en_US.UTF-8

dependencies:
  override:
    - brew update
    - brew install llvm

test:
  override:
    - utils/build-script
    - swift utils/run-examples.swift
